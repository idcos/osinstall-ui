import utilsFileUploader from '../../../utils/utils/file-uploader';
import { module, test } from 'qunit';

module('Unit | Utility | utils/file uploader');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = utilsFileUploader();
  assert.ok(result);
});
